/**
 * 部署配置 —— 全项目唯一需要手工填的地方（多数情况下连这里都不用填）
 *
 * cloudEnv：云开发环境 ID
 *   · 从「微信开发者工具 → 云开发控制台 → 环境 → 环境 ID」复制，形如 haofan-3g1a2b3c4d
 *   · 必须显式填写！新版「免费体验版」云环境不会被识别为默认环境，
 *     留空会直接报 -501000「Environment not found, there is no default environment exists」
 *   · 有多个云环境（测试/生产等）时：填上你要用的那一个
 */
module.exports = {
  cloudEnv: 'cloud1-d1g8yreir12ed382c',
};
